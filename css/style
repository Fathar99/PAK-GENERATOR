:root{
  --ink: #1b2430;
  --ink-soft: #4b5563;
  --line: #d8dee6;
  --paper: #f6f7f9;
  --card: #ffffff;
  --navy: #1e3a5f;
  --navy-deep: #16293f;
  --gold: #b8863b;
  --good: #2f7a4f;
  --bad: #b3441f;
  --radius: 10px;
  font-size: 15px;
}

*{ box-sizing: border-box; }
html,body{ margin:0; padding:0; }
body{
  font-family: "Source Sans Pro", "Segoe UI", Arial, sans-serif;
  background: var(--paper);
  color: var(--ink);
  line-height: 1.45;
}

h1,h2,h3{ font-family: "Georgia", "Times New Roman", serif; color: var(--navy-deep); margin: 0 0 12px; }
h2{ font-size: 1.15rem; border-bottom: 2px solid var(--navy); padding-bottom: 8px; }
h3{ font-size: 0.98rem; margin-top: 18px; color: var(--navy); }

.topbar{ background: var(--navy-deep); color:#fff; }
.topbar-inner{
  max-width: 1280px; margin: 0 auto; padding: 14px 24px;
  display:flex; align-items:center; justify-content:space-between; gap: 16px;
}
.brand{ display:flex; align-items:center; gap: 14px; }
.brand-mark{
  font-family: Georgia, serif; font-weight:700; font-size: 1.4rem;
  background: var(--gold); color: var(--navy-deep);
  width: 44px; height: 44px; border-radius: 8px;
  display:flex; align-items:center; justify-content:center;
  flex-shrink: 0;
}
.brand-text strong{ display:block; font-size: 1.02rem; }
.brand-text small{ color: #c7d2e0; font-size: 0.8rem; }
.topbar-actions{ display:flex; gap:8px; }

.layout{
  max-width: 1280px; margin: 0 auto; padding: 24px;
  display:grid; grid-template-columns: 1fr 360px; gap: 24px; align-items:start;
}
@media (max-width: 980px){
  .layout{ grid-template-columns: 1fr; }
}

.card{
  background: var(--card); border: 1px solid var(--line); border-radius: var(--radius);
  padding: 20px 22px; margin-bottom: 20px;
}
.sticky{ position: sticky; top: 20px; }

label{ display:block; font-size: 0.86rem; font-weight:600; color: var(--ink-soft); margin-bottom: 12px; }
label span.muted, .muted{ font-weight:400; color:#8a94a3; font-size: 0.8em; }
input, select{
  width:100%; margin-top: 5px; padding: 9px 10px;
  border: 1px solid var(--line); border-radius: 6px;
  font-size: 0.92rem; font-family: inherit; color: var(--ink);
  background:#fff;
}
input:focus, select:focus{ outline: 2px solid var(--navy); outline-offset: 1px; }

.grid-2{ display:grid; grid-template-columns: 1fr 1fr; gap: 0 18px; }
.grid-3{ display:grid; grid-template-columns: 1fr 1fr 1fr; gap: 0 18px; }
.grid-4{ display:grid; grid-template-columns: 1fr 1fr 1fr 1fr; gap: 0 18px; }
@media (max-width: 640px){
  .grid-2, .grid-3, .grid-4{ grid-template-columns: 1fr; }
}

.hint{ font-size: 0.82rem; color: #6b7684; margin: 4px 0 14px; }

.switch-row{ display:flex; align-items:center; gap:10px; font-weight:600; }
.switch-row input{ width:auto; margin:0; }

.hidden{ display:none; }

.readout{ background: var(--paper); border-radius: 6px; padding: 10px 14px; font-size:0.88rem; margin-top: 22px; }
.readout div{ margin-bottom: 4px; }

.periode-row{
  border: 1px solid var(--line); border-radius: 8px; padding: 14px 16px; margin-bottom: 12px;
  background: #fbfcfd;
  position: relative;
}
.periode-row .grid-2, .periode-row .grid-3{ gap: 0 14px; }
.periode-row label{ font-size: 0.8rem; margin-bottom: 8px; }
.periode-row .row-head{ display:flex; justify-content:space-between; align-items:center; margin-bottom:10px; }
.periode-row .row-title{ font-weight:700; color: var(--navy); font-size: 0.85rem; }
.periode-ak{ font-size: 0.82rem; color: var(--good); font-weight:700; margin-top: 4px; }

.btn{
  border:none; border-radius: 7px; padding: 10px 16px; font-weight:700; font-size:0.9rem;
  cursor:pointer; font-family: inherit;
}
.btn-primary{ background: var(--navy); color:#fff; }
.btn-primary:hover{ background: var(--navy-deep); }
.btn-secondary{ background: #eef2f6; color: var(--navy-deep); border: 1px solid var(--line); }
.btn-secondary:hover{ background:#e3e9ef; }
.btn-ghost{ background: transparent; color:#fff; border: 1px solid rgba(255,255,255,0.35); }
.btn-ghost:hover{ background: rgba(255,255,255,0.1); }
.btn-remove{ background: transparent; color: var(--bad); border:none; font-weight:700; cursor:pointer; font-size:0.8rem; }
.btn-block{ width:100%; margin-top: 14px; }

.preview-table{ width:100%; border-collapse: collapse; font-size: 0.83rem; margin-bottom: 10px; }
.preview-table td{ padding: 6px 4px; border-bottom: 1px solid var(--line); vertical-align: top; }
.preview-table td:last-child{ text-align:right; font-weight:700; white-space:nowrap; }

.preview-total{
  display:flex; justify-content:space-between; font-size:0.95rem;
  border-top: 2px solid var(--navy); padding-top: 10px; margin-bottom: 14px;
}
.preview-block{ font-size: 0.83rem; background: var(--paper); border-radius:6px; padding: 10px 12px; margin-bottom: 6px; }
.preview-block div{ display:flex; justify-content:space-between; margin-bottom:4px; }
.preview-block .conclusion{ margin-top: 8px; font-weight:700; color: var(--navy-deep); }

#generate-status{ margin-top: 8px; text-align:center; }
#generate-status.ok{ color: var(--good); }
#generate-status.err{ color: var(--bad); }

.footer{ max-width:1280px; margin: 0 auto; padding: 10px 24px 40px; color:#8a94a3; font-size:0.78rem; }
